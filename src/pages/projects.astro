---
import { getCollection } from 'astro:content';
import BaseLayout from '@/layouts/BaseLayout.astro';
import ProjectCard from '@/components/ProjectCard.astro';
import { getMessages } from '@/i18n/messages';

const t = getMessages('en').projects;
const projects = await getCollection('projects');

const sorted = [...projects.filter((p) => p.data.featured), ...projects.filter((p) => !p.data.featured)];

const title = 'Projects – Alex John Chamba';
---

<BaseLayout lang="en" pathname={Astro.url.pathname} title={title}>
  <section class="section projects-page">
    <span class="section-label">work</span>
    <h1 class="section-title">
      {t.myRecent}<span class="accent">{t.works}</span>
    </h1>
    <p class="subtitle">A selection of projects I've built — from web apps to data tools.</p>

    <div class="projects-grid">
      {
        sorted.map((project) => (
          <ProjectCard
            name={project.data.name.en}
            description={project.data.description.en}
            tech={project.data.tech}
            image={project.data.imageProjectLink}
            projectLink={project.data.projectLink}
            demoLink={project.data.demoLink || undefined}
            featured={project.data.featured}
          />
        ))
      }
    </div>
  </section>
</BaseLayout>

<style>
  .projects-page {
    padding-top: clamp(1.5rem, 4vw, 3rem);
  }

  .subtitle {
    color: var(--text-muted);
    font-size: 1rem;
    margin-bottom: 2.5rem;
    max-width: 55ch;
  }

  .accent {
    color: var(--accent);
  }

  .projects-grid {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: 1fr;
  }

  @media (min-width: 640px) {
    .projects-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
